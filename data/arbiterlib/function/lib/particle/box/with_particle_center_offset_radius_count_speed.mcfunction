$particle $(particle) ~$(center_offset) ~-$(center_offset) ~ 0 0 $(radius) $(speed) $(count)
$particle $(particle) ~$(center_offset) ~$(center_offset) ~ 0 0 $(radius) $(speed) $(count)
$particle $(particle) ~-$(center_offset) ~-$(center_offset) ~ 0 0 $(radius) $(speed) $(count)
$particle $(particle) ~-$(center_offset) ~$(center_offset) ~ 0 0 $(radius) $(speed) $(count)

$particle $(particle) ~ ~-$(center_offset) ~$(center_offset) $(radius) 0 0 $(speed) $(count)
$particle $(particle) ~ ~$(center_offset) ~$(center_offset) $(radius) 0 0 $(speed) $(count)
$particle $(particle) ~ ~-$(center_offset) ~-$(center_offset) $(radius) 0 0 $(speed) $(count)
$particle $(particle) ~ ~$(center_offset) ~-$(center_offset) $(radius) 0 0 $(speed) $(count)

$particle $(particle) ~$(center_offset) ~ ~$(center_offset) 0 $(radius) 0 $(speed) $(count)
$particle $(particle) ~-$(center_offset) ~ ~$(center_offset) 0 $(radius) 0 $(speed) $(count)
$particle $(particle) ~$(center_offset) ~ ~-$(center_offset) 0 $(radius) 0 $(speed) $(count)
$particle $(particle) ~-$(center_offset) ~ ~-$(center_offset) 0 $(radius) 0 $(speed) $(count)
